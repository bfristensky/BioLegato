REV - Reverses the text on each line in the standard pipeline
 
This command reads in data from standard in, and reverses the text on each line.
 
COMMAND LINE ARGUMENTS
----------------------
This command does not accept any command line arguments.
 
EXECUTION STATUS
----------------
This command returns one (1) unless an exception is encountered.  In the case of an exception, this command will return zero (0)
 
@author Graham Alvare
@author Brian Fristensky