RM - This command deletes files
 
COMMAND LINE ARGUMENTS
----------------------
This command accepts any number of command line arguments.  The command line arguments should be formatted in the following manner:
 
rm [flags] file(s)
 
Flags:

 -r-R   # delete recursively
 
EXECUTION STATUS
----------------
This command returns one (1) unless an exception is encountered.  In the case of an exception, this command will return zero (0)
 
@author Graham Alvare
@author Brian Fristensky